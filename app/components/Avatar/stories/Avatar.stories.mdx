import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Avatar from '..';
import { Avatar as avatarProp } from '..';

<Meta title="Components|Avatar" component={Avatar} />

# Avatar Images

<Props of={avatarProp} />

## UserAvatar

(userAvatar appears on the top right of the page, it's fixed, no need to enter size info)

<Preview>
  <div />
  <Story name="userAvatar">
    <Avatar id="loggedIn" />
  </Story>
</Preview>

## WriterAvatar

(writerAvatar has a size attr for now, which is chosen from a fixed list, this API will be changed in the future)

<Preview>
  <Story name="writerAvatar">
    <Avatar id="ID001" size="80px" />
  </Story>
  <Story name="writerAvatar-nonmember">
    <Avatar id="ID001" size="80px" />
  </Story>
</Preview>
