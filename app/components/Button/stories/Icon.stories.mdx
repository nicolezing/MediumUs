import _ from 'lodash';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { IconButton, GlowIconButton } from '..';
import * as Icons from '../../../staticData/svgIcons/IconButton_Icons';

<Meta title="Components|Button/icon" component={IconButton} />

# Icon Button

<Props of={IconButton} />

## Icons

<Preview>
  <Story name="icons">
    {_.keys(Icons).map(icon => (
      <div>
        <span>{icon}</span>
        <br />
        <IconButton iconName={icon} theme="pureBlack" />
        <br />
      </div>
    ))}
  </Story>
</Preview>

## Effects

(Doesn't work properly in storybook Docs, check Canvas)

<Preview>
  <Story name="glow">
    <GlowIconButton iconName="clapIcon" theme="pureBlack" effect="glow" />
  </Story>
</Preview>

## Examples

<Preview>
  <IconButton iconName="logoIcon" theme="pureBlack" />
  <IconButton iconName="twIcon" theme="gray" />
  <IconButton iconName="moreIcon" theme="gray" />
  <IconButton iconName="moreHollowIcon" theme="pureBlack" />
  <IconButton iconName="clapIcon" theme="purple" effect="glow" />
</Preview>
