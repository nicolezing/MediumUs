import _ from 'lodash';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { IconButton } from '..';
import * as Icons from '../../../staticData/images/icons';

<Meta title="Components|Button/icon" component={IconButton} />

# Icon Button

<Props of={IconButton} />

## Icons

<Preview>
  <Story name="icons">
    {_.keys(Icons).map(icon => (
      <div>
        <span>{icon}</span>
        <br />
        <IconButton type={icon} colorSet="pureBlack" />
        <br />
      </div>
    ))}
  </Story>
</Preview>

## Effects

(Doesn't work properly in storybook Docs, check Canvas)

<Preview>
  <Story name="glow">
    <IconButton type="clapIcon" colorSet="pureBlack" effect="glow" />
  </Story>
</Preview>

## Examples

<Preview>
  <IconButton type="logoIcon" colorSet="pureBlack" />
  <IconButton type="twIcon" colorSet="gray" />
  <IconButton type="moreIcon" colorSet="gray" />
  <IconButton type="moreHollowIcon" colorSet="pureBlack" />
  <IconButton type="clapIcon" colorSet="purple" effect="glow" />
</Preview>
